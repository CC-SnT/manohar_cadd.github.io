const menuBtn=document.getElementById("menuBtn");const navLinks=document.getElementById("navLinks");menuBtn?.addEventListener("click",()=>navLinks.classList.toggle("open"));document.getElementById("year").textContent=new Date().getFullYear();const pubs=[...document.querySelectorAll(".publication")];const yearFilter=document.getElementById("yearFilter");[...new Set(pubs.map(p=>p.dataset.year))].sort((a,b)=>b-a).forEach(y=>{const o=document.createElement("option");o.value=y;o.textContent=y;yearFilter.appendChild(o)});const searchInput=document.getElementById("searchInput");function filterPubs(){const q=searchInput.value.toLowerCase().trim();const y=yearFilter.value;pubs.forEach(p=>{const okText=p.textContent.toLowerCase().includes(q);const okYear=y==="all"||p.dataset.year===y;p.style.display=okText&&okYear?"block":"none"})}searchInput.addEventListener("input",filterPubs);yearFilter.addEventListener("change",filterPubs);